# App
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# Database (SQLite by default - no C++ build required on Windows)
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0
alembic==1.14.0
# For PostgreSQL (DDEV/Docker): pip install asyncpg then set DATABASE_URL=postgresql://...

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Redis and Celery
redis==5.2.1
celery[redis]==5.4.0

# HTTP and APIs
# ollama 0.3.3 requires httpx<0.28
httpx>=0.27.0,<0.28.0
# Use range so pip can pick a build with pre-built wheels (avoids pydantic-core source build)
pydantic>=2.5.0,<3
pydantic-settings>=2.2.0,<3
email-validator>=2.0.0

# Listen Notes / external
# pytrends for Google Trends (no key)
pytrends==4.9.2
# PRAW for Reddit (optional)
praw==7.8.1
# YouTube Data API
google-api-python-client==2.149.0

# Ollama client (local LLM + embeddings)
ollama==0.3.3

# Utils
python-dotenv==1.0.1
